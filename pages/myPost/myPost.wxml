<!--pages/myPost/myPost.wxml-->
<view class="container">
  <PostList>
      <PostItem
      wx:for="{{postList}}"
        wx:key="_id"
        bindlongpress="onDelete" 
        id="{{item._id}}" 
        postItem="{{item}}" 
        />
  
  </PostList>
</view>
